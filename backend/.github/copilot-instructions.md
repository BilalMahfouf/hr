# Copilot Instructions

## General Guidelines
- First general instruction
- Second general instruction

## Code Style
- Use specific formatting rules
- Follow naming conventions

## Project-Specific Rules
- User prefers flat response DTOs with all fields at the root level instead of nested records. For example: (AnimalId, AnimalName, ...) instead of (AnimalInfo Animal, ...). This applies to all GetById and GetAll endpoint responses.